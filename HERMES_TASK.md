# Hermes 任务书：基金 Tab 分类策略 V2

> 本文件只是一份执行任务书，不是代码。Hermes 应分阶段执行，每个阶段完成后先报告并停止，未经用户确认不得自动进入下一阶段。

## 1. 项目位置

```text
C:\Users\thfwj\Documents\Codex\2026-08-03\chatgpt-codex-1-codex-github-ah9102ah\investment-signal
```

仓库：

```text
https://github.com/ah9102ah-cmyk/investment-signal
```

开始前必须完整阅读：

```text
AGENTS.md
HANDOFF.md
README.md
```

## 2. 开发纪律

1. 当前工作区可能位于 `main`，开始开发前必须刷新远端状态，并把最新 `main` 安全同步到 `dev`。
2. 所有开发、研究和提交只在 `dev`。
3. 不直接修改、合并或推送 `main`。
4. 不触发线上 workflow，除非用户之后明确批准上线。
5. 不恢复 `signal-email` 和 `holdings-email` 的定时邮件。
6. 不修改股票 Tab 的策略含义。
7. 不读取账户并自动下单，不输出个性化仓位或交易数量。
8. 页面允许显示固定规则的“买入 / 持有 / 卖出”，但必须同时给出判断依据。
9. 所有指数必须坚持“结构/基本面 + 价格技术面”双轮确认。
10. 缺失数据不得静默按 0 分处理。
11. 凭证不得进入代码、数据文件、日志或提交。
12. 每个阶段完成后报告结果并停止，等待用户确认。

## 3. 总目标

将基金 Tab 从：

```text
所有指数基本共用一套技术参数
+ 部分指数使用不同估值口径
```

逐步改造成：

```text
统一数据质量和风险底线
        ↓
5 类指数策略引擎
        ↓
统一买入 / 持有 / 卖出结果
        ↓
类内优先级和风险集中提示
```

必须保留当前线上策略：

```text
common_v1
```

新策略先命名为：

```text
category_v2_shadow
```

新策略不得直接替换当前正式结果。只有经过研究、测试和 8～12 周影子观察后，某一类别才可以单独考虑切换到 `category_v2`。

## 4. 信号对象边界

当前页面给出的是指数观察信号，不是具体 ETF 的个性化交易指令。

必须区分：

- 指数层：估值、行业或宏观环境、指数趋势、市场宽度。
- ETF 层：跟踪误差、折溢价、汇率、管理费、成立日期、交易成本。

第一版分类策略只解决指数层。不得把具体 ETF 的盘中折溢价直接混入指数信号。若以后需要具体 ETF 执行层，必须另立任务。

## 5. 五类指数策略

### 5.1 宽基核心类

包含：

- 沪深300
- 中证500

核心逻辑：

```text
自身历史估值 + 中长期趋势 + 波动风险 + 可选市场宽度
```

要求：

- 沪深300继续重视 PE、PB、股债利差。
- 中证500降低估值权重，提高趋势、市场宽度和波动控制的重要性。
- 中证500不能机械套用沪深300的买入门槛。
- 每类最多研究三套有经济含义的候选：估值优先、均衡、趋势优先。

候选权重范围仅用于研究，不得直接认定为最终参数：

| 指标 | 沪深300 | 中证500 |
|---|---:|---:|
| 估值 | 35%～45% | 20%～30% |
| 中长期趋势 | 35%～45% | 45%～55% |
| 短期动能 | 10%～15% | 10%～20% |
| 市场宽度/质量 | 10%～15% | 10%～20% |

### 5.2 红利因子类

包含：

- 红利低波

核心逻辑：

```text
PE 历史分位 + 股息率历史分位 + 股息相对国债收益率 + 慢趋势保护
```

要求：

- 技术行情使用前复权 ETF 或可靠全收益口径。
- 不得因分红除权误判为下跌。
- 短期 5 日涨跌的重要性降低。
- 股息率历史不足时允许降级到 PE，但必须明确标注。
- 趋势主要用于防止在持续下跌中仅因“便宜”过早进入。

候选权重范围：

| 指标 | 权重 |
|---|---:|
| PE 与股息估值 | 45%～60% |
| 中长期趋势 | 25%～40% |
| 短期动能 | 5%～15% |

### 5.3 高成长科技类

包含：

- 科创50
- 恒生科技

核心逻辑：

```text
中长期趋势 + 市场宽度/盈利环境 + 自身估值护栏 + 波动控制
```

要求：

- 不能简单因为 PE 高就直接判定卖出。
- 估值必须相对自身历史，不能与沪深300横向比较。
- 优先研究成分股位于长期均线以上的比例、上涨成分股比例以及上涨是否由少数权重股推动。
- 盈利预测上调比例只有在历史数据可靠、发布时间可验证时才可加入。
- 恒生科技缺少可靠历史估值时不得伪造 `V=0`。
- 若估值不可用，可由可靠的盈利、市场宽度或流动性指标承担结构轮，但必须标明替代依据和降级状态。
- 高波动指数必须采用波动率标准化，不能与红利低波共用固定涨跌阈值。

候选权重范围：

| 指标 | 权重 |
|---|---:|
| 中长期趋势 | 40%～55% |
| 市场宽度/盈利环境 | 20%～35% |
| 估值护栏 | 10%～25% |
| 短期动能 | 10%～20% |

### 5.4 行业主题类

包含：

- 中证白酒
- 中证医疗

共同逻辑：

```text
行业估值 + 盈利周期 + 市场宽度 + 趋势确认
```

白酒重点：

- PE 历史分位。
- 成分股利润增速和盈利下调比例。
- 行业上涨家数。
- 趋势是否真正止跌。
- 库存或渠道指标只有在数据长期稳定后才考虑。

医疗重点：

- PE 历史分位。
- 成分股盈利改善比例。
- 行业上涨家数。
- 上涨是否由少数大权重股推动。
- 政策风险只可作为明确标签，不得使用主观新闻情绪直接打分。

共同限制：

- “估值便宜 + 继续下跌”不能触发买入。
- 至少需要结构轮与价格轮共同确认。
- 行业基本面缺失时，不允许仅靠超跌输出买入。

候选权重范围：

| 指标 | 权重 |
|---|---:|
| 行业估值 | 25%～40% |
| 盈利周期/市场宽度 | 25%～40% |
| 中长期趋势 | 20%～35% |
| 短期动能 | 5%～15% |

### 5.5 实物资产类

包含：

- 黄金

黄金不得使用股票 PE 逻辑。建议将黄金的结构指标定义为宏观环境分 `G`。

核心逻辑：

```text
实际利率/美元等宏观环境 + 黄金中长期趋势 + 短期过热提示
```

候选宏观指标：

- 美国10年期实际利率。
- 广义美元指数。
- 通胀预期。
- 央行购金或黄金 ETF 流量只能在数据稳定后再考虑。

要求：

- 相对 250 日均线的位置不能同时计入“估值”和“趋势”。
- 宏观环境和价格趋势必须保持独立。
- 宏观数据缺失或陈旧时不得输出强买入。
- 黄金技术行情继续使用可靠的前复权黄金 ETF 数据。

候选权重范围：

| 指标 | 权重 |
|---|---:|
| 宏观环境 | 35%～50% |
| 中长期趋势 | 35%～50% |
| 短期动能/过热 | 10%～20% |

## 6. 统一信号协议

各类别策略应返回统一的数据合同。页面不得自行复制另一套业务判断。

建议字段：

```text
strategy_version
strategy_type
asset_name
valuation_score
macro_score
trend_score
momentum_score
quality_score
structural_score
technical_score
composite_score
action
action_reason
data_quality
degraded_fields
class_rank
risk_cluster
risk_warning
signal_date
valuation_date
price_date
```

分项分数统一限制为：

```text
-2, -1, 0, +1, +2
```

但必须注意：

- 各类别综合分不一定能机械横向比较。
- 科技类 `S=4` 与红利类 `S=4` 不代表相同把握。
- 原始综合分主要解释单个指数自身状态。
- 跨类别排序应使用风险调整后的趋势、结构确认程度和数据完整度。

## 7. 双轮判断

### 第一轮：结构轮

| 类别 | 结构轮 |
|---|---|
| 沪深300 | PE、PB、股债利差 |
| 中证500 | 自身估值、市场宽度 |
| 红利低波 | PE、股息率、股息债券利差 |
| 科技 | 自身估值或盈利/宽度/流动性 |
| 行业 | 行业估值、盈利周期 |
| 黄金 | 实际利率、美元等宏观环境 |

### 第二轮：价格轮

统一包含：

- 中长期趋势方向。
- 多周期趋势共识。
- 波动率标准化动能。
- 过热/超跌风险提示。

禁止：

- RSI 超卖直接等于买入。
- 单日上涨直接等于趋势反转。
- 缺结构轮时仅靠价格轮输出强买入。
- 同一个均线指标在两个轮次重复加分。

## 8. 波动率标准化

这是 V2 的第一优先级。

当前固定近 5 日涨跌阈值不适合所有指数。研究以下定义：

```text
5日标准化变化
=
近5日收益 ÷（近20或60日每日波动率 × √5）
```

只研究少量有解释力的候选阈值：

```text
±0.5 倍波动
±1.0 倍波动
±1.5 倍波动
```

不得连续搜索大量精确阈值。

RSI 调整为：

- 用于确认动能。
- 用于提示过热或超跌。
- 不单独决定买卖。
- 极端 RSI 不反向覆盖中长期趋势。

## 9. 最终三态含义

### 买入

必须同时满足：

1. 结构轮不弱。
2. 中长期趋势多数向上。
3. 短期动能没有明显拖后腿。
4. 关键数据达到最低完整度。
5. 没有触发类别特有禁止条件。

### 卖出

满足以下任一类：

- 中长期趋势多数向下，短期趋势同步转弱。
- 结构轮明显恶化，价格轮也转弱。
- 综合弱项达到预先定义的退出门槛。

数据异常不得直接触发卖出，只能显示数据不足或降级。

### 持有

没有触发买入或卖出时显示持有。

页面必须继续说明：

```text
已有仓位：持有表示暂不改变。
未持有：持有表示继续观望。
```

## 10. 组合层风险提示

建立风险簇：

| 风险簇 | 指数 |
|---|---|
| 大盘权益 | 沪深300 |
| 中小盘权益 | 中证500 |
| 防守/红利 | 红利低波 |
| 高成长科技 | 科创50、恒生科技 |
| 消费行业 | 中证白酒 |
| 医疗行业 | 中证医疗 |
| 实物资产 | 黄金 |

组合层只输出观察提示和类内排名，不输出具体仓位。

规则：

1. 同一风险簇出现多个买入时，给出类内优先级。
2. 科创50和恒生科技同时买入时，提示科技风险集中。
3. 沪深300与白酒同时偏强时，提示可能存在成分暴露重叠。
4. 多个权益指数同时卖出时，提示权益整体偏弱。
5. 黄金作为分散资产单独展示，但不承诺一定对冲权益风险。
6. 不读取用户账户，不生成个性化仓位。

## 11. 数据纪律

### 行情数据

- 可投资收益研究优先使用 ETF 前复权行情。
- 估值使用对应指数数据。
- ETF 成立前的数据必须标为指数模拟研究期。
- 不得把指数历史收益冒充真实 ETF 可交易收益。
- 不同市场休市日使用所选日期之前最近有效交易日。
- 禁止未来数据填充。

### 财报和盈利数据

- 必须按披露日使用，而不是按报告期结束日提前使用。
- 盈利预测必须保留当时版本；若无法取得历史快照，不得用于严格历史回测。

### 市场宽度和成分股

- 不得使用今天的成分股列表回填全部历史。
- 若无法取得可靠历史成分股，第一版只能使用当前截面观察，不得宣称有完整历史回测。
- 第一版优先采用简单且可验证的宽度指标：长期均线以上比例、近20日上涨比例、等权与市值加权方向差异。

### 外部数据

- 每个外部数据源均需缓存。
- 拉取失败时可以使用最近有效缓存，但必须标注陈旧程度。
- 超过预设陈旧期限后不得给强信号。
- 记录数据源、观察日期、有效日期和降级原因。
- 评估 GitHub Actions 的网络可用性、速率限制和运行时间，不得只验证本地可用。

## 12. 分阶段执行

### 阶段 A：研究和数据审计

本阶段是 Hermes 首次执行的唯一范围。

任务：

1. 复现当前 `common_v1` 的规则、页面结果和回测结果。
2. 审计 5 类策略所需数据源。
3. 对每个数据源记录：来源、历史长度、发布时间、更新频率、GitHub Actions 可达性、是否需要凭证、缓存方式、失败降级方式。
4. 明确哪些数据可稳定使用，哪些只能列为后续候选。
5. 每类只设计三套候选：估值优先、均衡、趋势优先。
6. 明确无未来数据、披露日、历史成分股和 ETF 成立日期处理方式。
7. 形成 `STRATEGY_V2_PLAN.md` 和 `STRATEGY_V2_RESEARCH.md`。
8. 本阶段不得改变网页正式输出。
9. 完成后向用户报告并停止。

阶段 A 交付内容：

- 当前基准是否可复现。
- 数据源可用性表。
- 各类候选策略定义。
- 回测设计。
- 风险与无法实现项。
- 是否建议进入阶段 B。

### 阶段 B：最小可行影子版本

只有用户确认阶段 A 后才能开始。

任务：

1. 建立 5 类策略配置层。
2. 保留 `common_v1`。
3. 实现 `category_v2_shadow`。
4. 优先完成波动率标准化。
5. 完成黄金宏观环境与趋势拆分。
6. 完成统一数据质量和降级逻辑。
7. 只使用阶段 A 已证明稳定的数据源。
8. 页面展开区并排展示正式结果和影子结果。
9. 不改变当前正式三态。
10. 完成测试后报告并停止。

### 阶段 C：增强版本

只有用户确认阶段 B 后才能开始。

任务：

- 在数据可靠时加入历史市场宽度。
- 在有历史快照时加入盈利周期或盈利修正指标。
- 增加类内排名和风险集中提示。
- 保持不输出具体仓位。
- 为 8～12 周影子观察准备记录和对比。

阶段 C 不等于正式上线。是否替换某一类别仍需单独决定。

## 13. 回测方案

每类策略必须同时比较：

1. 当前 `common_v1`。
2. 始终持有。
3. 简单 200 日趋势。
4. 估值单因子。
5. `category_v2_shadow`。

统一假设：

- 周频判断。
- 信号次日执行。
- 单边成本：0.1%、0.3%、0.5%。
- 信号延迟：1～5个交易日。
- 至少测试5种不同周内观察日。
- ETF 成立前的数据单独标注。
- 不使用未来估值、未来财报或当前成分股回填历史。

输出指标：

- 年化收益。
- 最大回撤。
- 年化波动率。
- 风险收益指标。
- 下行波动。
- 平均参与比例。
- 年换手率。
- 盈亏次数。
- 最长连续错误期。
- 分年份表现。
- 牛市、熊市、震荡期表现。
- 成本敏感度。
- 延迟敏感度。
- 参数扰动稳定性。

## 14. 防过拟合

禁止：

- 大量连续权重搜索。
- 针对单个指数设置大量精确阈值。
- 因某一年表现不好单独修改规则。
- 只报告最佳参数。
- 把已经参与选模的 2025、2026 数据继续称为严格样本外。

必须报告：

- 全部候选方案结果。
- 参数 ±20% 后的结果。
- 删除最佳年份后的结果。
- 不同市场阶段结果。
- 失败案例。
- 选模过程和研究污染风险。
- 今后需要影子验证的部分。

## 15. 建议验收门槛

分类策略进入影子模式前建议满足：

1. 大多数滚动区间不差于 `common_v1`。
2. 最大回撤改善至少2个百分点，或风险收益指标提高至少0.1。
3. 年化收益不能比 `common_v1` 低超过2个百分点。
4. 单边成本提高到0.5%后不能完全失效。
5. 信号延迟1～5天后结论基本稳定。
6. 参数 ±20% 后没有从明显有效变成明显无效。
7. 不能依赖单一年份贡献大部分优势。
8. 换手率没有不合理增加。
9. 数据缺失和降级流程正常。
10. Python、网页和其他共用规则的输出一致。

历史不足时应降低结论置信度，不得放宽标准后宣称有效。

## 16. 影子观察

影子模式至少观察 8～12 周，记录：

- 每周 `common_v1` 与 `category_v2_shadow` 的输出。
- 两者是否一致以及差异原因。
- 后续1周、4周、12周表现。
- 信号是否频繁反复。
- 数据源失败频率。
- 分类策略是否改善错误判断或回撤。
- 哪些类别有效，哪些没有明显改善。

允许按类别逐步替换。没有稳定改善的类别继续沿用 `common_v1`。

## 17. 页面建议

基金 Tab 每行可考虑显示：

```text
指数名称
策略类型
V/G
T
M
S
最终结果
数据质量
```

展开后显示：

- 正式策略结果。
- 影子策略结果。
- 两者是否一致。
- 结构轮结论。
- 价格轮结论。
- 类别特有指标。
- 缺失或陈旧数据。
- 类内优先级。
- 风险集中提示。
- 当前策略版本。

文案必须使用大白话。

## 18. 测试清单

### 单元测试

- 8个指数正确归类。
- 各类分数边界。
- 波动率为0。
- 数据不足。
- 数据陈旧。
- 历史估值不可用。
- 黄金宏观数据缺失。
- 多周期趋势分歧。
- 买入、持有、卖出边界。
- 缺数据不能静默当0。
- 黄金不重复计分。
- 恒生科技替代结构轮。
- 类内排名。
- 风险集中提示。

### 一致性测试

- Python 与网页结果一致。
- 根目录和 `web` 页面副本一致。
- `data/fund.json` 与 `web/data/fund.json` 一致。
- 历史日期不使用当前估值。
- 估值日期、行情日期和披露日期正确对齐。

### 浏览器测试

- 桌面与手机布局。
- 8个指数都有明确状态。
- 展开理由可读。
- 数据缺失标签清楚。
- 切换历史日期正常。
- 控制台无错误。
- 基金和股票 Tab 不互相影响。

## 19. 回滚和版本控制

- `common_v1` 必须保持可独立运行。
- `category_v2_shadow` 必须通过显式策略版本或功能开关启用。
- 新数据字段需要 schema/version 标记，并兼容旧数据。
- 新数据源失败时应回退到 `common_v1` 或显示降级，不得让整个页面不可用。
- 上线时应能通过单一版本开关恢复旧策略，不依赖大规模代码回滚。

## 20. Hermes 最终交付物

每阶段报告至少包含：

1. 完成范围。
2. 未完成范围。
3. 修改文件清单。
4. 数据源清单和可用性。
5. 候选参数表。
6. 完整回测对比。
7. 成本、延迟和扰动结果。
8. 失败案例。
9. 测试结果。
10. 页面截图（阶段 B 以后）。
11. 已知风险。
12. 是否建议进入下一阶段。

建议文档：

```text
STRATEGY_V2_PLAN.md
STRATEGY_V2_RESEARCH.md
HANDOFF.md
README.md
strategy.html
```

## 21. 提交与上线流程

Hermes 完成阶段开发后：

1. 确认分支为 `dev`。
2. 检查没有敏感凭证。
3. 运行全部相关测试。
4. 本地启动网页验收。
5. 提交到 `dev`。
6. 未经用户明确授权，不推送或只按用户授权推送 `dev`。
7. 向用户报告，不主动合并 `main`。
8. 用户过目并明确批准后才能合并 `main`。
9. 正式上线后手动触发 `update-fundamentals`。
10. 确认 workflow 成功回写数据。
11. 确认 GitHub Pages 刷新。
12. 次日检查09:00自动任务。
13. 邮件定时任务继续保持关闭。

## 22. Hermes 首次执行指令

Hermes 第一次读取本文件后，只执行阶段 A。

不要一开始追求最高收益。优先保证：

- 分类有经济逻辑。
- 数据不穿越。
- 信号不重复计分。
- 参数不过拟合。
- 数据源可在 GitHub Actions 稳定运行。
- 当前 `common_v1` 可以完整保留和复现。

阶段 A 完成后必须报告并停止，等待用户发送：

```text
阶段A方案认可，继续阶段B，只做影子版本，不上线。
```
